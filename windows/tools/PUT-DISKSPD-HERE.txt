Extract DiskSpd.zip (https://github.com/microsoft/diskspd/releases) into this folder,
so that tools\amd64\diskspd.exe (or tools\DiskSpd\amd64\diskspd.exe) exists.
Server-DiskBench.ps1 searches the kit folder recursively for diskspd.exe.
